import { getMinMaxDistributiveLattice } from './DistributiveLattice';
/**
 * @since 1.4.0
 */
export const getMinMaxBoundedDistributiveLattice = (O) => (min, max) => {
    return Object.assign({}, getMinMaxDistributiveLattice(O), { zero: min, one: max });
};
